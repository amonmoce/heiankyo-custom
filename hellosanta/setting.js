/**
 * Created by cobenash on 16/1/26.
 */

// (function($) {
    
function (date) {
                    var day = date.getDay();
                    if (day == 1 || day == 0) {
                        return [false];
                    } else {
                        return [true];
                    }

                };
                minDate = +1;
            }
        }
    };
})(jQuery);
